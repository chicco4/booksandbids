integrate cors library

integrate the new jsons as models
start by integrating user with new fields
    no first i integrate the other models and code basic functionality

add a scheduler code that closes auctions if time comes